import { ProductList } from '../modules/home/pages/ProductList';
import { Layout } from '../components/Layout';

const Main: React.FC = () => {
    return (
        <Layout>
            <ProductList />
        </Layout>
    );
};

export default Main;
